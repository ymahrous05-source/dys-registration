# خطوات تركيب الباك اند (Google Apps Script) — النسخة الجديدة

## ⚠️ اقرأ الأول — عندك بيانات حقيقية شغالة

قبل أي حاجة، افتح **الشيت الحقيقي** اللي عليه تسجيلات الأعضاء الفعلية، وروح على:

`Extensions` (الإضافات) ▸ `Apps Script`

- الكود القديم موجود هناك فعلاً (النسخة اللي كانت شغالة قبل كده). **متمسحوش ومتستبدلوش بيه على طول** — اعمل نسخة احتياطية الأول: `File` ▸ `Make a copy`، واشتغل بالنسخة دي للتجربة قبل ما تلمس الحقيقية.
- الكود الجديد ده **بديل كامل** للكود القديم، وبيشتغل على نفس الشيت والأعمدة بالظبط — مفيش أي داعي تغيّر أي حاجة في بياناتك الحالية. أول ما ترفعه هيفضل شغال بنفس التبويب (الشيت) اللي كنت شغال عليه، لحد ما تستخدم ميزة "دورة تسجيل جديدة" الجديدة (تحت).

## اللي اتضاف في النسخة دي

1. **ميعاد للتسجيل**: تحدد بداية ونهاية للتسجيل. برة الميعاد ده، الفورم بيقفل نفسه تلقائيًا ويعرض للزوار رسالة "لسه ما بدأش" أو "قفل" بدل الاستمارة.
2. **شيت جديد لكل دورة تسجيل**: لما تفتح تسجيل جديد (بعد ما يقفل القديم)، تقدر تعمل "دورة جديدة" من لوحة التحكم، فيتعمل تلقائيًا تبويب/شيت جديد في نفس ملف الجوجل شيت، وكل التسجيلات الجديدة بتتسجل فيه — من غير ما يتلمس أي شيء في الدورة اللي فاتت. تقدر ترجع تشوف بيانات أي دورة قديمة من لوحة التحكم برضه.
3. **عنوان استمارة قابل للتغيير**: تقدر تحدد اسم/عنوان يظهر في أعلى الاستمارة، من غير ما تلمس كود الـ HTML خالص.
4. **صورة/شعار قابل للتغيير**: تقدر ترفع صورة من جهازك (بدل الشعار الافتراضي) من لوحة التحكم مباشرة.

كل الإعدادات دي بتتحكم فيها من تبويب **⚙️ الإعدادات** الجديد جوه `dys_dashboard.html` — مفيش داعي تعدّل كود خالص عشان تستخدمها.

---

## 1) افتح Apps Script

من نفس الشيت: `Extensions` ▸ `Apps Script`.

## 2) الصق الكود

امسح الكود القديم في `Code.gs` والصق محتوى ملف **Code.gs** المرفق مكانه.

## 3) اتأكد إن أسماء الأعمدة متطابقة

نفس الأعمدة القديمة بالظبط:

```
Timestamp | Membership No | Name | Age | Gender | National ID | Phone |
Whatsapp | Email | Address | Faculty | Graduate | Role in Entity |
Has Job | Current Job
```

- **لو الشيت فاضي**: هيتعمل تلقائيًا أول ما حد يسجل، أو شغّل الدالة `setupSheet` يدويًا (خطوة 5).
- **لو الشيت فيه بيانات حقيقية بالفعل**: شغّل الدالة `checkHeaders` وشوف الـ Logs.

## 4) حدد اسم الشيت (Tab) الحالي

في أول الكود:

```js
const SHEET_NAME = "Registrations";
```

غيّرها لاسم التبويب الحقيقي عندك دلوقتي (اللي فيه بياناتك الحالية).

## 5) شغّل دوال الإعداد مرة واحدة

من قائمة الدوال فوق، شغّل بالترتيب:

1. `setupSheet`
2. `checkHeaders`
3. `setAdminPassword_` — افتحها، غيّر `NEW_PASSWORD` لكلمة سر قوية، شغّلها مرة، وبعدين امسح كلمة السر من الكود لو حابب.

## 6) اعمل Deploy

`Deploy` ▸ `New deployment`:

- Type: **Web app**
- Execute as: **Me**
- Who has access: **Anyone**

هتاخد رابط شكله:
`https://script.google.com/macros/s/AKfycb.../exec`

## 7) حدّث الرابط في الفورم والداشبورد

في **أول** ملفي `dys_form.html` و `dys_dashboard.html`:

```js
const SCRIPT_URL = "الرابط_الجديد_بتاعك_هنا";
```

> لو الرابط القديم لسه شغال ومقتنع إنه نفس الشيت، ممكن تعمل **Manage deployments ▸ Edit (✏️) ▸ New version ▸ Deploy** بدل Deployment جديد — كده الرابط يفضل زي ما هو.

## 8) اختبار قبل ما تعتمد عليه فعليًا

1. افتح `dys_form.html` وسجّل ببيانات وهمية، وتأكد إنه ظهرلك رقم عضوية.
2. روح على الشيت وشوف الصف اتضاف صح.
3. سجّل بنفس الرقم القومي تاني — لازم يقولك "أنت مسجل بالفعل".
4. افتح `dys_dashboard.html`، سجّل دخول، وتأكد إن البيانات ظاهرة.
5. امسح صف الاختبار الوهمي من الشيت يدويًا بعد التأكد.

---

## 9) استخدام ميعاد التسجيل + الدورات الجديدة (الميزة الجديدة)

من `dys_dashboard.html`، سجّل دخول، وروح لتبويب **⚙️ الإعدادات**:

- **عنوان الاستمارة**: أي حاجة تكتبها هنا تظهر فورًا كعنوان استمارة التسجيل.
- **اسم الشيت الأساسي**: الاسم اللي هيتبني عليه اسم أي شيت/تبويب جديد (مثلاً لو كتبت "تسجيلات"، الدورة الأولى هتبقى "تسجيلات"، والتانية "تسجيلات 2"، وهكذا).
- **بداية/نهاية التسجيل**: حدد تاريخ ووقت. سيبهم فاضيين لو عايز التسجيل يفضل مفتوح من غير حد.
  - ⚠️ التوقيت بيتحسب محليًا. لو مش متأكد من توقيت الـ Apps Script، افتح **Project Settings ▸ Time zone** جوه محرر الكود وتأكد إنه **Africa/Cairo** (أو المنطقة الزمنية اللي بتشتغلوا بيها).
- زرار **حفظ الإعدادات**: بيحفظ العنوان والتواريخ من غير ما يعمل شيت جديد.
- زرار **ابدأ دورة تسجيل جديدة**: بيعمل تبويب/شيت جديد فعلاً، ويخلي التسجيلات الجديدة تتسجل فيه، من غير ما يلمس بيانات الدورة اللي فاتت خالص. استخدمه لما تفتح باب تسجيل جديد بعد ما القديم يكون قفل.
- **قائمة "الدورة الحالية"** فوق: تقدر تختار أي دورة قديمة وتضغط "عرض" عشان تشوف بياناتها في الجدول والرسوم البيانية.

الفورم نفسه (`dys_form.html`) بيتأكد من الميعاد ده كل مرة حد يفتحه، وبيقفل نفسه أو يعرض عد تنازلي تلقائيًا برة الميعاد.

## 10) تغيير صورة/شعار الاستمارة

من نفس تبويب **⚙️ الإعدادات**، تحت "صورة الاستمارة (الشعار)":

1. اختار صورة من جهازك (فايل → اختيار صورة).
2. اضغط **رفع الصورة**.
3. الصورة هتظهر فورًا بدل الشعار الافتراضي لأي حد يفتح استمارة التسجيل بعد كده.
4. لو حبيت ترجع للشعار الأصلي، اضغط **استخدام الشعار الافتراضي**.

الصورة بتتحفظ في Google Drive بتاع حساب صاحب الـ Apps Script (نفس حساب الشيت)، بصلاحية "أي حد معاه الرابط يقدر يشوفها" — عشان تقدر تظهر في استمارة عامة بدون تسجيل دخول.

---

## 11) شهادات الأعضاء (PDF بيتبعت على الإيميل)

الميزة دي بتاخد ملف Word بتصميم الشهادة، وكل مرة عضو يحتاج شهادة بتعمل نسخة منه، تحط بياناته مكان الأماكن المتغيرة، تصدرها PDF، وتبعتها على إيميله — من غير ما تلمس أصل ملف الـ Word بتاعك خالص.

### أ) لازم تفعّل خدمة Drive API مرة واحدة بس

الخاصية دي بتحتاج تحويل ملف الـ Word (.docx) لـ Google Doc عشان تقدر تعدّل فيه، وده محتاج خدمة إضافية:

1. من محرر Apps Script، جنب كلمة **Services** في الشريط الجانبي الشمال، دوس على **+**.
2. دور على **Drive API** واختارها، وسيب الإصدار زي ما هو، ودوس **Add**.
3. كده خلاص، الخدمة دي هتفضل مفعّلة للمشروع من غير ما تعمل حاجة تانية.

لو نسيت الخطوة دي، أول ما ترفع تيمبلت الشهادة هيطلعلك رسالة خطأ بتقولك ترجع تفعّلها.

### ب) جهّز ملف الشهادة (Word)

اعمل التصميم بتاعك عادي في Word زي ما انت متعود بالظبط، وحط جوه المكان اللي المفروض يتغيّر لكل عضو النص ده **بالظبط** (بالإنجليزي، جوه قوسين مزدوجين):

| النص اللي تكتبه في الـ Word | هيتحول لإيه |
|---|---|
| `{{name}}` | اسم العضو |
| `{{membershipNo}}` | رقم العضوية (DYS-000123) |
| `{{age}}` | السن |
| `{{gender}}` | النوع (Male/Female) |
| `{{nationalId}}` | الرقم القومي |
| `{{phone}}` | رقم الهاتف |
| `{{whatsapp}}` | رقم الواتساب |
| `{{email}}` | الإيميل |
| `{{faculty}}` | الكلية |
| `{{graduate}}` | خريج ولا لأ (Yes/No) |
| `{{role}}` | الصفة/الدور داخل الكيان |
| `{{hasJob}}` | بيشتغل ولا لأ (Yes/No) |
| `{{currentJob}}` | الوظيفة الحالية |
| `{{registrationDate}}` | تاريخ التسجيل الأصلي (من الشيت) |
| `{{date}}` | تاريخ إرسال الشهادة (لحظة الإرسال، مش وقت التسجيل) |

تقدر تحط أي عدد منهم في أي مكان في الشهادة (جوه صندوق نص، جدول، أي فونت أو لون)، وسيب الباقي زي ما هو. مش لازم تستخدمهم كلهم — انت اللي بتحدد مين يظهر وفين بالظبط.

⚠️ اكتبهم بالظبط كده — نفس الأقواس `{{ }}` ونفس الاسم بالحروف الصغيرة، من غير مسافات جواهم.

### ج) ارفع التيمبلت من الداشبورد

من `dys_dashboard.html` ▸ تبويب **⚙️ الإعدادات** ▸ كارت **🎓 شهادات الأعضاء**:

- اختار ملف الـ `.docx` واضغط **رفع التيمبلت**.
- كل مرة ترفع ملف جديد، بيستبدل القديم — يعني لو الشهادة بتختلف شكلها كل دورة، تقدر ترفع تصميم جديد وقتها من غير أي تعديل في الكود.
- اضغط **ابعت تجريبية** وحط إيميلك، عشان تتأكد إن الشكل والبيانات ظاهرة صح قبل ما تعتمد عليها.

### د) امتى الشهادة تتبعت؟

- **أوتوماتيك**: فعّل زرار "إرسال الشهادة أوتوماتيك لكل عضو فور ما يسجل" — هتتبعت مع/بعد إيميل تأكيد التسجيل مباشرة.
- **يدوي لعضو واحد**: من الجدول في الداشبورد، دوس على أي صف عشان يفتح تفاصيله، وهتلاقي زرار "📧 إرسال شهادة للعضو ده".
- **يدوي لكل الدورة**: من نفس كارت الشهادات، زرار "📧 إرسال شهادات لكل أعضاء الدورة المختارة فوق" بيبعت لكل عضو معاه إيميل جوه الدورة اللي مختارها من قايمة "الدورة الحالية" فوق.

كل الطرق التلاتة بتستخدم نفس التيمبلت المرفوع، وأي عضو مفيهوش إيميل بيتم تخطيه تلقائيًا (بيظهرلك عدده في نتيجة الإرسال الجماعي).

---

## 12) التحكم في حقول الاستمارة (تظهر/تختفي، إجباري/اختياري)

من `dys_dashboard.html` ▸ تبويب **⚙️ الإعدادات** ▸ كارت **🧩 حقول الاستمارة**:

- جدول فيه كل الحقول القابلة للتغيير: السن، النوع، الهاتف، الواتساب، الإيميل، الكلية، خريج؟، الصفة داخل الكيان، بيشتغل؟.
- عمود **ظاهر**: لو مقفول، الحقل بيختفي تمامًا من الاستمارة (والتحقق بتاعه بيتلغي في الباك اند كمان، مش بس في الشكل).
- عمود **إجباري**: شغّال بس لو "ظاهر" مفعّل. لو مقفول، الحقل بيفضل موجود بس اختياري (زي الإيميل بالظبط دلوقتي).
- اضغط **حفظ إعدادات الحقول** — التغيير بيسري على استمارة التسجيل فورًا من غير أي Deploy جديد.

⚠️ **"الاسم" و"الرقم القومي" مش موجودين في الجدول ده عمدًا** — النظام كله (منع التكرار، رقم العضوية) بيعتمد عليهم، فهيفضلوا ظاهرين وإجباريين دايمًا مهما عملت.

الأعمدة في الشيت (Timestamp, Age, Gender, Phone... إلخ) بتفضل موجودة زي ما هي حتى لو قفلت حقل معين — بس هتبقى فاضية للتسجيلات الجديدة. البيانات القديمة اللي كانت متسجلة قبل ما تقفل الحقل مش بتتمسح.

---

## 13) لو الشهادة التجريبية بترجع "Exception: ليس لديك إذن لاستدعاء DocumentApp.openById"

الخطأ ده **مختلف تمامًا** عن "Drive API مش مفعّلة" (لو كانت المشكلة دي، هتلاقي `driveApiAdvancedService` في التشخيص بيقول "غير مفعّلة"). لو `driveApiAdvancedService` شغالة و`certTemplateReady: true` بس لسه الإرسال بيفشل بالخطأ ده تحديدًا، السبب إن تفعيل خدمة **Drive API** كـ Advanced Service بيحوّل المشروع لوضع "صلاحيات محددة يدويًا" في ملف `appsscript.json`، فبيوقف عن اكتشاف الصلاحيات المطلوبة تلقائيًا من الكود — وصلاحية `DocumentApp` (اللي بيحتاجها `generateCertificatePdf_()` عشان يفتح نسخة الشهادة ويملاها) بتضيع من غير ما تحس.

**الحل:**

1. من محرر Apps Script: **Project Settings** (أيقونة الترس) ▸ فعّل **"Show 'appsscript.json' manifest file in editor"**.
2. من قائمة الملفات الشمال، افتح **appsscript.json**.
3. دور على مصفوفة `oauthScopes`، وتأكد إن فيها السطر ده (ضيفه لو مش موجود):
   ```json
   "https://www.googleapis.com/auth/documents"
   ```
   لو المصفوفة كلها مش موجودة أصلاً، ضيفها بالكامل جوه الـ JSON:
   ```json
   "oauthScopes": [
     "https://www.googleapis.com/auth/spreadsheets",
     "https://www.googleapis.com/auth/drive",
     "https://www.googleapis.com/auth/documents",
     "https://www.googleapis.com/auth/script.send_mail"
   ]
   ```
4. احفظ الملف.
5. **لازم تعيد الموافقة (re-authorize)**: ارجع لـ `Code.gs`، اختار أي دالة من القايمة فوق (مثلاً `setupSheet`) ودوس ▷ Run — هتظهرلك نافذة أذونات فيها الصلاحية الجديدة، وافق عليها (لو ظهرت رسالة "Google hasn't verified this app"، دوس Advanced ▸ Go to project (unsafe) — طبيعي لأنه مشروعك الشخصي).
6. جرب **ابعت تجريبية** تاني من الداشبورد.

هتلاقي فحص جديد اسمه `documentAppScope` في **🩺 تشخيص** بيتأكد من الصلاحية دي بالظبط، فمش هتحتاج تستنى الخطأ ده يطلع من الإرسال الفعلي تاني.

---

## 14) صلاحية إضافية لازمة لـ QR code وتصدير Excel

ميزتين جداد بيحتاجوا صلاحية `script.external_request` (عشان بيعملوا طلب لخدمة خارجية: مولّد الـ QR، وتصدير الشيت كـ Excel):
- QR code على الشهادة (لو التيمبلت فيه `{{qrcode}}`)
- زرار "📊 تصدير Excel منسّق" في الداشبورد

لو حصلت مشكلة معاهم، افتح `appsscript.json` (زي قسم 13 فوق) وتأكد إن `oauthScopes` فيها:
```json
"https://www.googleapis.com/auth/script.external_request"
```
واعمل نفس خطوات إعادة الموافقة (شغّل أي دالة من المحرر ووافق على الصلاحية الجديدة).

**عشان تستخدم QR code في أي تيمبلت شهادة:** حط `{{qrcode}}` في مكان الشهادة اللي عايز الكود يظهر فيه، وارفع التيمبلت زي المعتاد — الكود هيتحط تلقائيًا مكان الكلمة دي وقت الإرسال. لو التيمبلت مفيهوش `{{qrcode}}` أصلاً، مفيش أي تغيير، هيشتغل عادي زي قبل كده.

**صورة العضو في الشهادة:** بنفس الطريقة بالظبط، حط `{{photo}}` في مكان الشهادة اللي عايز صورة العضو تظهر فيه (لازم يكون العضو فعّل ورفع صورة في الاستمارة، وتكون خانة "صورة شخصية" مفعّلة من "🧩 حقول الاستمارة").

---

## ⚠️ لو حاجة مش شغالة (إعدادات مش بتتحفظ، إيميل مش بيوصل...)

أشهر سبب بالترتيب:

1. **مش عامل Deploy جديد بعد ما لصقت الكود الجديد.** تعديل الكود في المحرر لوحده **مش كافي** — لازم:
   `Deploy` ▸ `Manage deployments` ▸ دوس على قلم التعديل (✏️) ▸ `Version: New version` ▸ `Deploy`.
   لو عملت Deploy جديد تمامًا (رابط مختلف) بدل تحديث القديم، لازم تحدّث `SCRIPT_URL` في أول `dys_form.html` و
   `dys_dashboard.html` بالرابط الجديد، وإلا هيفضل الفورم شغال على الكود القديم.

2. **مفعّلتش Drive API** (لو بتستخدم ميزة الشهادات) — شوف قسم 11-أ فوق.

3. **حصة الإيميل اليومية خلصت** (Gmail عادي ~100 إيميل/يوم) — لو مليتها، أي إيميل جديد هيفشل بصمت.

عشان تعرف السبب بالظبط من غير تخمين، من الداشبورد ▸ ⚙️ الإعدادات ▸ كارت **🩺 تشخيص**، دوس **شغّل التشخيص**. هيوريك:
- `codeVersion`: لو مش نفس القيمة الموجودة في أول `Code.gs` اللي انت لصقته، يبقى لسه شغال على نسخة قديمة — ارجع لخطوة 1.
- `mailRemainingQuota`: لو 0، دي حصة الإيميل خلصت.
- `driveApiAdvancedService`: لو مكتوب "غير مفعّلة"، روح فعّلها.
- `documentAppScope`: لو بيقول "بترمي خطأ"، ده تحديدًا مشكلة صلاحية `DocumentApp` — شوف قسم 13 فوق للحل.
- `fieldConfig`: القيم الفعلية المحفوظة دلوقتي على السيرفر — قارنها باللي انت واثق إنك حفظته.

---

## ملاحظات مهمة

- **الإيميلات**: بترسل من خلال حساب الجيميل بتاع صاحب الـ Apps Script (`MailApp`)، وفيه حد أقصى يومي (~100 إيميل لحساب Gmail عادي، أكتر لو Google Workspace). لو فعّلت إرسال الشهادة الأوتوماتيك، كل تسجيل هيبقى بيستهلك إيميلين (تأكيد + شهادة) من نفس الحد اليومي ده — خد بالك لو عندك دفعة تسجيلات كبيرة في يوم واحد.
- **الإرسال الجماعي للشهادات** بيبعت كل الإيميلات واحد ورا التاني في نفس الطلب، فلو عدد الأعضاء كبير جدًا (كذا مية) ممكن العملية تاخد وقت أو توصل لحد وقت التنفيذ بتاع Apps Script (6 دقايق) — في الحالة دي قسّم الإرسال على أكتر من مرة، أو ابعته لأعضاء أقل في كل مرة.
- **كلمة سر الداشبورد** بتتبعت في رابط الطلب (query string) — نفس التصميم الأصلي.
- **أرقام العضوية** (DYS-000001، DYS-000002، ...) بتستمر في العد حتى لو بدأت دورة تسجيل جديدة — يعني الرقم مش بيتصفر مع كل دورة.
- **التكرار بالرقم القومي**: بيتفحص جوه الدورة/الشيت الحالي بس. لو حد سجل في دورة قديمة وحاول يسجل تاني في دورة جديدة، هيتقبل — لأن كل دورة تسجيل مستقلة تمامًا عن اللي قبلها.
